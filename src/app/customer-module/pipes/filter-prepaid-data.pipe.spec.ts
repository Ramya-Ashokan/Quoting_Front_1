import { FilterPrepaidDataPipe } from './filter-prepaid-data.pipe';

describe('FilterPrepaidDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPrepaidDataPipe();
    expect(pipe).toBeTruthy();
  });
});
