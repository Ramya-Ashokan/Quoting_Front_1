import { FilterPrepaidUnlimitedPipe } from './filter-prepaid-unlimited.pipe';

describe('FilterPrepaidUnlimitedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPrepaidUnlimitedPipe();
    expect(pipe).toBeTruthy();
  });
});
