import { FilterPrepaidTalkTimePipe } from './filter-prepaid-talk-time.pipe';

describe('FilterPrepaidTalkTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPrepaidTalkTimePipe();
    expect(pipe).toBeTruthy();
  });
});
