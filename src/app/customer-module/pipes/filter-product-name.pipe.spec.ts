import { FilterProductNamePipe } from './filter-product-name.pipe';

describe('FilterProductNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductNamePipe();
    expect(pipe).toBeTruthy();
  });
});
